// export const toggleSelect = (counters, setCounters, id) => {
//   const indexOfCounter = counters.indexOf(counter => counter.id === id);
//   console.log(indexOfCounter);

//   return setCounters([
//     ...counters,
//     counters[indexOfCounter] : {

//     }
//     (counters.find(counter => counter.id === id).selected = !counters.find(
//       counter => counter.id === id
//     ).selected)
//   ]);
// };

// // const counter = counters.find(counter => counter.id === id);
// // console.log(counter.selected);
// // counter.selected = !counter.selected;
// // console.log(counter.selected);
